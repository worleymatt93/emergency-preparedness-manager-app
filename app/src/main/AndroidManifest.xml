<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.emergencypreparednessmanager">

  <!-- Permissions -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:resizeableActivity="true"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsPictureInPicture="false"
    android:supportsRtl="true"
    android:theme="@style/Theme.EmergencyPreparednessManager">

    <!-- Launcher Activity -->
    <activity
      android:exported="true"
      android:launchMode="singleTop"
      android:name=".ui.activities.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Other Activities (alphabetical order) -->
    <activity android:name=".ui.activities.KitEditActivity" />
    <activity android:name=".ui.activities.KitItemEditActivity" />
    <activity android:name=".ui.activities.KitItemsActivity" />
    <activity android:name=".ui.activities.KitListActivity" />
    <activity android:name=".ui.activities.ReportsActivity" />
    <activity android:name=".ui.activities.SearchSuppliesActivity" />
    <activity android:name=".ui.activities.SettingsActivity" />


    <!-- File Provider CSV sharing -->
    <provider
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true"
      android:name="androidx.core.content.FileProvider">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>

    <!-- Broadcast Receiver for scheduled notifications -->
    <receiver
      android:exported="false"
      android:name=".ui.receivers.AlertReceiver" />

  </application>

</manifest>