<?xml version="1.0" encoding="utf-8"?>
<paths>

  <!-- Grants access to files in getExternalFilesDir() -->
  <external-files-path
    name="external_files"
    path="." />

  !-- Grants access to files in getFilesDir() -->
  <files-path
    name="internal_files"
    path="." />

</paths>